using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AILogicTester : MonoBehaviour {

    /// <summary>
    /// Reads in a file, then tells the AI Controller to read
    /// it in and say which piece it will move
    /// </summary>
    /// <param name="textFile">file to read in</param>
    public void RunBoardTest(TextAsset textFile)
    {

    }
}
